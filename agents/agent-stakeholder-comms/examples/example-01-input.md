# Input: Release note request

Output type: release note
Audience: product designers and frontend engineers
DS version: v1.2.0
Changes:
- Button component: added `size=xl` variant and `state=loading` for all emphasis levels
- Badge component: new component (sm/md × 5 intents × filled/outline)
- Color tokens: renamed `$color/brand/500` to `$color/primary/default` — breaking change
