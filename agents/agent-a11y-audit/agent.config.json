{
  "model": "claude-sonnet-4-6",
  "description": "WCAG AA accessibility auditor — uses figma-cli to run contrast, touch-target, and focus-order checks.",
  "figma_cli": {
    "daemon_url": "http://localhost:3456",
    "connection_mode": "yolo",
    "allowed_commands": [
      "fig-start a11y audit",
      "fig-start a11y contrast",
      "fig-start a11y touch",
      "fig-start a11y text",
      "fig-start a11y vision"
    ]
  },
  "env": {
    "FIGMA_CLI_PORT": "3456"
  },
  "notes": [
    "figma-cli connects via local CDP daemon — no Figma API token required.",
    "Run 'figma-cli connect' (Yolo mode) before invoking this agent.",
    "For CI use: set connection_mode to 'safe' and configure the Figma plugin."
  ]
}
