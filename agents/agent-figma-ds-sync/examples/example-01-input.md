# Input: Token export request

Export all color and spacing tokens from the active Figma file.
Output format: Style Dictionary JSON.
Compare against the previous export at `tokens/tokens.json` if it exists.
