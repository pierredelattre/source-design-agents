# Input: DS question

"When should I use an overlay (modal/dialog) versus an inline alert to communicate an error to the user?"

Context: web app, the error occurs after a form submission fails due to a server error.
