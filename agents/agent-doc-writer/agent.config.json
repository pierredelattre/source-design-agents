{
  "model": "claude-sonnet-4-6",
  "description": "Doc writer — generates functional specs and component docs from Figma data exported via figma-cli and screenshots via Bridge.",
  "figma_cli": {
    "daemon_url": "http://localhost:3456",
    "connection_mode": "yolo",
    "allowed_commands": [
      "fig-start canvas info",
      "fig-start export"
    ]
  },
  "bridge": {
    "mcp_server": "plugin:bridge-ds:figma-console",
    "requires_setup": false,
    "use": "screenshot-verify-only",
    "verify_command": "figma-cli verify \"NODE_ID\""
  },
  "env": {
    "FIGMA_CLI_PORT": "3456",
    "DOCS_OUTPUT_PATH": "TODO: path to write generated documentation (e.g. 'docs/components')"
  },
  "notes": [
    "figma-cli is used for canvas inspection and component data export.",
    "Bridge verify is used for screenshots included in docs — not for creation.",
    "Run 'figma-cli connect' before first use.",
    "Set DOCS_OUTPUT_PATH before automated runs."
  ]
}
