# Input: Screen description

**Screen:** Settings — Profile section
**Platform:** Web (desktop), responsive down to 375px
**User goal:** View and edit their display name, email, and avatar.

## Zones

1. Page header with title "Settings" and a back link
2. Profile section: avatar (editable), display name field, email field (read-only with a "Change email" link)
3. Save button (primary, disabled until a field changes)
4. Danger zone: "Delete account" link (destructive, text only)
