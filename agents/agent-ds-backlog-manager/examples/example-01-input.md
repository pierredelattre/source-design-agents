# Input: Component request list

1. Toast notification component — needed by 3 product teams for async feedback
2. Data table with sortable columns — requested by the analytics team
3. Avatar with initials fallback — needed in 2 features already being designed
4. New Button variant: `size=xl` — requested by one designer for a hero CTA
5. Tooltip — "would be nice to have" per a developer
6. Primary button with icon — same as existing Button/Primary but with a leading icon
